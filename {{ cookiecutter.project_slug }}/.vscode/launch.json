{
  "configurations": [
    {
      "command": "yarn install -D; yarn run build; yarn run package; yarn run test",
      "name": "Build, Package and Test",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
