{
  "configurations": [
    {
      "command": "npm install -D; npm run build; npm run package; npm run test",
      "name": "Build, Package and Test",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
